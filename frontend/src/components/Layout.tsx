import { Link, NavLink, Outlet, useNavigate } from 'react-router-dom';
import { Home, MessageCircle, PlusSquare, Search, Settings, LogOut, Moon, Sun, UserRound } from 'lucide-react';
import { useAuth } from '../store/auth';
import { useState } from 'react';

export function Layout(){ const {user,logout}=useAuth(); const navigate=useNavigate(); const [dark,setDark]=useState(()=>localStorage.getItem('socialcf_theme')==='dark'); const toggle=()=>{const next=!dark;setDark(next);document.documentElement.classList.toggle('dark',next);localStorage.setItem('socialcf_theme',next?'dark':'light');}; const nav=[['/',Home,'Лента'],['/chats',MessageCircle,'Чаты'],['/create',PlusSquare,'Создать'],['/settings',Settings,'Настройки']] as const; return <div className="min-h-screen bg-slate-50 text-slate-900 dark:bg-slate-950 dark:text-slate-100"><aside className="fixed inset-y-0 hidden w-64 border-r border-slate-200 bg-white/80 p-6 backdrop-blur dark:border-slate-800 dark:bg-slate-950/80 md:block"><Link to="/" className="mb-10 block text-2xl font-black tracking-tight">Social<span className="text-violet-500">CF</span></Link><nav className="space-y-2">{nav.map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold ${isActive?'bg-violet-100 text-violet-700 dark:bg-violet-950/60 dark:text-violet-300':'text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900'}`}><Icon size={19}/>{label}</NavLink>)}<NavLink to={`/u/${user?.username}`} className="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900"><UserRound size={19}/>Профиль</NavLink></nav><div className="absolute bottom-6 left-6 right-6 space-y-3"><button onClick={toggle} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900">{dark?<Sun size={18}/>:<Moon size={18}/>}Тема</button><button onClick={()=>{logout();navigate('/login')}} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900"><LogOut size={18}/>Выйти</button></div></aside><header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b border-slate-200 bg-white/90 px-4 backdrop-blur dark:border-slate-800 dark:bg-slate-950/90 md:hidden"><Link to="/" className="text-xl font-black">Social<span className="text-violet-500">CF</span></Link><div className="flex items-center gap-3"><Link to={`/u/${user?.username}`}><UserRound size={21}/></Link><button onClick={toggle}>{dark?<Sun size={19}/>:<Moon size={19}/>}</button></div></header><main className="mx-auto max-w-3xl px-4 py-8 md:ml-64 md:max-w-4xl md:px-10"><Outlet/></main><nav className="fixed bottom-0 left-0 right-0 z-10 flex h-16 items-center justify-around border-t border-slate-200 bg-white/95 px-3 backdrop-blur dark:border-slate-800 dark:bg-slate-950/95 md:hidden">{nav.slice(0,3).map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>`flex flex-col items-center gap-1 text-xs ${isActive?'text-violet-500':'text-slate-500'}`}><Icon size={21}/>{label}</NavLink>)}</nav></div> }
