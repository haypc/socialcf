import { Heart, MessageCircle } from 'lucide-react'; import { Link } from 'react-router-dom'; import { mediaUrl, postsApi } from '../api/client'; import type { Post } from '../api/types'; import { useState } from 'react';
export function PostCard({post}:{post:Post}){const [liked,setLiked]=useState(post.liked);const [likes,setLikes]=useState(post.likesCount); const toggle=async()=>{const {data}=await postsApi.like(post.id);setLiked(data.liked);setLikes(data.likesCount)}; return <article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center gap-3 p-4"><div className="grid h-10 w-10 place-items-center rounded-full bg-gradient-to-br from-violet-500 to-fuchsia-500 text-sm font-bold text-white">{post.author.username[0]?.toUpperCase()}</div><div><Link to={`/u/${post.author.username}`} className="font-semibold hover:text-violet-500">{post.author.displayName || post.author.username}</Link><div className="text-xs text-slate-400">@{post.author.username} · {new Date(post.createdAt).toLocaleDateString('ru-RU')}</div></div></div>{post.mediaKey&&<img src={post.mediaUrl||mediaUrl(post.mediaKey)||''} alt={post.caption||'Публикация'} className="max-h-[600px] w-full object-cover"/>}<div className="p-4"><div className="mb-3 flex items-center gap-4"><button onClick={toggle} className={liked?'text-rose-500':'text-slate-500'}><Heart size={22} fill={liked?'currentColor':'none'}/></button><Link to={`/p/${post.id}`} className="text-slate-500"><MessageCircle size={22}/></Link><span className="text-sm font-semibold">{likes} лайков</span></div>{post.caption&&<p className="text-sm leading-6"><span className="mr-2 font-semibold">{post.author.username}</span>{post.caption}</p>}<Link to={`/p/${post.id}`} className="mt-2 inline-block text-sm text-slate-400">Комментарии: {post.commentsCount}</Link></div></article>}
