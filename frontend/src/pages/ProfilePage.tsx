import { useEffect, useState } from 'react'; import { useParams } from 'react-router-dom'; import { usersApi } from '../api/client'; import type { Post, User } from '../api/types'; import { PostCard } from '../components/PostCard'; export function ProfilePage(){const {username=''}=useParams();const [profile,setProfile]=useState<User & {posts:Post[]}>();const [error,setError]=useState('');useEffect(()=>{usersApi.profile(username).then(r=>setProfile(r.data)).catch(()=>setError('Профиль не найден'))},[username]);if(error)return <p className="text-rose-500">{error}</p>;if(!profile)return <p>Загрузка…</p>;return <div><div className="mb-8 flex items-center gap-5 rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900"><div className="grid h-20 w-20 shrink-0 place-items-center rounded-full bg-gradient-to-br from-violet-500 to-fuchsia-500 text-3xl font-bold text-white">{profile.username[0]?.toUpperCase()}</div><div><h1 className="text-2xl font-black">{profile.displayName}</h1><p className="text-sm text-slate-500">@{profile.username}</p>{profile.bio&&<p className="mt-2 text-sm">{profile.bio}</p>}<div className="mt-3 flex gap-4 text-sm text-slate-500"><span><b className="text-slate-900 dark:text-white">{profile.posts.length}</b> постов</span><span><b className="text-slate-900 dark:text-white">{profile.followersCount||0}</b> подписчиков</span></div></div></div><div className="space-y-6">{profile.posts.map(p=><PostCard key={p.id} post={p}/>)}</div></div>}
