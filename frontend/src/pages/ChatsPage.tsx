import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { MessageCircle } from 'lucide-react'; import { chatsApi } from '../api/client'; import type { Chat } from '../api/types'; export function ChatsPage(){const [chats,setChats]=useState<Chat[]>([]);useEffect(()=>{chatsApi.list().then(r=>setChats(r.data.chats))},[]);return <div><div className="mb-8 flex items-center justify-between"><div><h1 className="text-3xl font-black">Чаты</h1><p className="mt-1 text-sm text-slate-500">Личные и групповые разговоры в реальном времени.</p></div><button className="btn-primary">Новый чат</button></div><div className="divide-y divide-slate-200 overflow-hidden rounded-2xl border border-slate-200 bg-white dark:divide-slate-800 dark:border-slate-800 dark:bg-slate-900">{chats.map(c=><Link key={c.id} to={`/chats/${c.id}`} className="flex items-center gap-4 p-4 hover:bg-slate-50 dark:hover:bg-slate-800"><div className="grid h-12 w-12 place-items-center rounded-full bg-violet-100 text-violet-600 dark:bg-violet-950"><MessageCircle size={21}/></div><div className="min-w-0 flex-1"><div className="font-semibold">{c.name||c.members?.map(m=>m.displayName).join(', ')||'Чат'}</div><p className="truncate text-sm text-slate-500">{c.lastMessage?.body||'Нет сообщений'}</p></div><span className="text-xs text-slate-400">{c.lastMessage&&new Date(c.lastMessage.createdAt).toLocaleDateString('ru-RU')}</span></Link>)}{!chats.length&&<p className="p-12 text-center text-slate-500">Чатов пока нет.</p>}</div></div>}
