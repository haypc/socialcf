import { useAuth } from '../store/auth'; export function SettingsPage(){const {user}=useAuth();return <div><h1 className="mb-8 text-3xl font-black">Настройки</h1><section className="rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900"><h2 className="font-bold">Аккаунт</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-slate-500">Имя пользователя</dt><dd className="mt-1 font-semibold">@{user?.username}</dd></div><div><dt className="text-slate-500">Email</dt><dd className="mt-1 font-semibold">{user?.email}</dd></div></dl></section></div>}
