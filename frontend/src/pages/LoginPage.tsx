import { FormEvent, useState, type ReactNode } from 'react'; import { Link, useLocation, useNavigate } from 'react-router-dom'; import { useAuth } from '../store/auth'; export function LoginPage(){const {login}=useAuth();const nav=useNavigate();const loc=useLocation();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();try{await login(email,password);nav((loc.state as any)?.from||'/')}catch(err:any){setError(err.response?.data?.error||'Не удалось войти')}};return <AuthShell title="С возвращением"><form onSubmit={submit} className="space-y-4"><input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Пароль" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/>{error&&<p className="text-sm text-rose-500">{error}</p>}<button className="btn-primary w-full">Войти</button></form><p className="mt-6 text-center text-sm text-slate-500">Нет аккаунта? <Link className="font-semibold text-violet-500" to="/register">Создать</Link></p></AuthShell>};
export function AuthShell({title,children}:{title:string;children:ReactNode}){return <div className="grid min-h-screen place-items-center bg-slate-50 px-4 dark:bg-slate-950"><div className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-xl dark:border-slate-800 dark:bg-slate-900"><div className="mb-8 text-center"><div className="mb-4 text-3xl font-black">Social<span className="text-violet-500">CF</span></div><h1 className="text-xl font-bold">{title}</h1></div>{children}</div></div>}
